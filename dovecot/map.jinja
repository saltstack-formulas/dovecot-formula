{% set dovecot = salt['grains.filter_by']({
    'Debian': {
        'config': {
            'local': '# no customization',
            'dovecotext': {},
            'confext': {},
            'conf': {},
            },
        'pkgs': ['imapd'],
    },
}, merge=salt['pillar.get']('dovecot:lookup')) %}
